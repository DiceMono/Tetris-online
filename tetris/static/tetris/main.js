const current = new Current();
const button = document.getElementById('start');
//button.addEventListener('click', current.playGame.bind(current))
//button.addEventListener('click', button.remove)
current.playGame()
